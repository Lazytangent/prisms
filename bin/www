#!/usr/bin/env node

const { port } = require('../config');
const app = require('../app');
const db = require('../db');

async function main() {
  app.listen(port, () => console.log(`Listening on port ${port}...`))
}

main();
